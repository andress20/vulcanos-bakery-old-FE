import { lightTheme } from './light'
import { darkTheme } from './dark'

const themes = {
  lightTheme,
  darkTheme,
}

export enum Themes {
  lightTheme = 'lightTheme',
  darkTheme = 'darkTheme',
}

export default themes
